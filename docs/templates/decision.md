## {{ID}} — {{TITLE}} ({{DATE}})

**Picked:** what we chose, in one or two sentences.

**Alternatives:** each one considered, and why it lost — a rejected alternative is not a
gap, so name them all.

**Constraint:** the actual thing that forced the choice (a deadline, a measurement, a
rule, a cost).

**Revisit if:** the condition under which this should be reopened. To reverse it later,
add a NEW decision marking this one **SUPERSEDED by Dn** — never edit this file.
