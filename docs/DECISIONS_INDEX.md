<!-- generated by ticketing-template from the decision files — do not edit; add a file -->
# Decisions

Append-only, numbered, one FILE each in `docs/decisions/` (`Dn-<slug>.md`):
what we picked · the alternatives and why each lost · the constraint · what would make
us revisit. To reverse one, add a new file marking the old **SUPERSEDED by Dn**.
New decision: `python3 scripts/wi.py decision new "<title>"` — same commit as the change.

Entries before the split live in the frozen log `docs/DECISIONS.md` (never edited).

- **D1** — Adopted the append-only decision log — [docs/DECISIONS.md](../docs/DECISIONS.md)
